apiVersion: v1
kind: Service
metadata:
  name: gitops-express-api-srv
  labels:
    app: gitops-express-api
  namespace: default
spec:
  selector:
    app: gitops-express-api
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
  type: ClusterIP
